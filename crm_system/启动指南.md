# 三层漏斗CRM系统 - 启动指南

## ⚠️ 重要提示

由于PowerShell执行策略限制，需要手动执行以下步骤。

## 🚀 启动步骤

### 步骤1：解除PowerShell执行策略限制（可选）

**以管理员身份**打开PowerShell，运行：

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### 步骤2：安装后端依赖

打开命令提示符（CMD）或PowerShell，执行：

```cmd
cd C:\Users\Qstrux\.gemini\antigravity\scratch\crm_system\backend
npm install
```

这将安装所有必要的依赖包（可能需要5-10分钟）。

### 步骤3：配置环境变量

```cmd
copy .env.example .env
```

然后编辑 `.env` 文件，配置数据库连接：

```env
DATABASE_URL="postgresql://postgres:password@localhost:5432/crm_db?schema=public"
```

**注意**：您需要先安装并启动PostgreSQL数据库。

### 步骤4：生成Prisma Client

```cmd
npx prisma generate
```

### 步骤5：运行数据库迁移

```cmd
npx prisma migrate dev --name init
```

### 步骤6：启动开发服务器

```cmd
npm run start:dev
```

服务将在 http://localhost:4000 启动。

### 步骤7：访问API文档

打开浏览器访问：
- **API文档**: http://localhost:4000/api-docs
- **健康检查**: http://localhost:4000/api

---

## 🐳 使用Docker（推荐）

如果您安装了Docker Desktop，可以一键启动所有服务：

```cmd
cd C:\Users\Qstrux\.gemini\antigravity\scratch\crm_system
docker-compose up -d
```

这将自动启动：
- PostgreSQL数据库
- Redis缓存
- 后端服务
- 前端服务（待开发）

查看服务状态：
```cmd
docker-compose ps
```

查看日志：
```cmd
docker-compose logs -f backend
```

停止服务：
```cmd
docker-compose down
```

---

## 🔧 故障排查

### 问题1：npm命令不可用

**解决方案**：安装Node.js
- 下载：https://nodejs.org/
- 推荐版本：Node.js 20 LTS

### 问题2：PostgreSQL未安装

**解决方案**：
1. 使用Docker（推荐）：`docker-compose up -d postgres`
2. 或下载安装：https://www.postgresql.org/download/

### 问题3：端口被占用

**解决方案**：
```cmd
# 查看端口占用
netstat -ano | findstr :4000

# 杀死进程
taskkill /PID <进程ID> /F
```

### 问题4：Prisma生成失败

**解决方案**：
```cmd
# 清理并重新生成
rmdir /s /q node_modules
npm install
npx prisma generate
```

---

## ✅ 验证安装

启动成功后，您应该看到：

```
╔═══════════════════════════════════════════════════════╗
║                                                       ║
║   🚀 三层漏斗CRM系统 - 后端服务已启动                ║
║                                                       ║
║   📍 服务地址: http://localhost:4000/api              ║
║   📚 API文档: http://localhost:4000/api-docs          ║
║   🌍 环境: development                                ║
║                                                       ║
╚═══════════════════════════════════════════════════════╝
```

---

## 📞 需要帮助？

如果遇到问题，请查看：
1. [GETTING_STARTED.md](./GETTING_STARTED.md) - 详细启动指南
2. [SUMMARY.md](./SUMMARY.md) - 项目概述
3. 后端日志输出

---

**祝您启动成功！** 🎉
